{% extends 'accounts/base.html.twig' %}

{% block contentpage %}
<div class="panel panel-default form-central"
	style="width: 70%; margin-left: auto; margin-right: auto; margin-top: 3%;">
	<h3>Área Administrativa -> Exibir Cetegorias</h3>
    <p></p>
	<table id="table" class="display">

		<thead>
			<tr>
				<th>ID</th>
				<th>Bairro</th>
				<th>Valor R$</th>
			</tr>
		</thead>
		<tbody>
			{% for delivery in deliveries %}
			<tr>
				<td>
					<a href="{{ path('edit_delivery', {'id': delivery.id}) }}"
					 title="Editar">
					{{ delivery.id }}
					</a>
				</td>
				<td>{{ delivery.neighborhood }}</td>
				<td>{{ delivery.value }}</td>
			</tr>
			{% endfor %}
		</tbody>
	</table>
</div>
{% endblock %}
